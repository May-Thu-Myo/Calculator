A calculator with JavaScript, HTML, and CSS. Javascript is used to create buttons to populate into the DOM and add click handlers to these buttons. For styling, the grid layout is used to organize the calculator buttons.


